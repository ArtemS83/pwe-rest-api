# pwe-rest-api

# TODO REST API

* **URL**

  <https://pwe-rest-api.herokuapp.com/api/todos>
  
* **Methods:**

   * `GET`  `'/'` : get all todos
   * `POST`  `'/'` : add new todo
   * `DELETE`  `'/:id'` : delete todo
   * `PATCH`  `'/:id/status'` : update status todo
